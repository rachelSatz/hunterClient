<p class="h4 text-center grey-color col-md-12">
<i class="fa fa-paper-plane fa-2x plane"></i></p>
<p class="h4 text-center grey-color col-md-12">{{ data.text }}</p>
<p class="h4 text-center grey-color col-md-12">העידכון ישלח לכתובות המייל:</p>
  <table class="table table-bordered table-striped data-table">
    <thead>
      <tr>
  
        <th>שם </th>
        <th>מייל</th>
  


      </tr>
    </thead>
    <tbody>
      <tr *ngIf="data.Contact.length===0">
    
        <td colspan="13" style="text-align:center; background-color: #eceeef; color: #0c0c0c;
	width: 100%">

          לא נמצאו נתונים
        </td>
      </tr>
      <tr  class=" overTr" *ngFor="let item of data.Contact" >
     
        <td >{{ item.firstName + ' ' + item.lastName }}</td>
        <td >{{ item.email }}</td>
   
      </tr>
    </tbody>
  </table>
  <div *ngIf="data.Contact.length===0">
      <a class="data-table-action" (click)="openFormDialog()">
      <span class="action-logo">
        <i class="fa fa-user"></i>
      </span>
        <span>הוסף איש קשר</span>
      </a>
    </div>

<p class="text-center blue-color">הנך מועבר לדף הבית</p>
