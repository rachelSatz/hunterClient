<div class="container-wide">
  <p class="title">פירוט תשלומים</p>

     <i class="fa fa-chevron-right fa-lg background3"  (click)="GetrouterLink()"
    matTooltip="חזור" matTooltipPosition="above"
       [matTooltipClass]="'mytooltip'"
    ></i>
  <div class="d-flex justify-content-center py-4 my-3 box-shadow">
    <div class="ml-5">
      <span class="light-grey-color">שם המעסיק:</span>
      <span class="grey-color">{{ process.employer.name }}</span>
    </div>
    <div class="ml-5">
      <span class="light-grey-color">ח.פ:</span>
      <span class="grey-color">{{ process.employer.businessNumber }}</span>
    </div>
    <div class="ml-5">
      <span class="light-grey-color">שם התהליך:</span>
      <span class="grey-color">{{ process.addToProcessName }}</span>
    </div>
    <div class="ml-5">
      <span class="light-grey-color">מספר התהליך:</span>
      <span class="grey-color">{{ process.id }}</span>
    </div>
    <div class="ml-5">
      <span class="light-grey-color">סכום לתשלום:</span>
      <span class="green-color">{{ process.details.paymentSum }} &#8362;</span>
    </div>
    <div>
      <span class="light-grey-color">מספר רשומות:</span>
      <span class="blue-color">{{ process.details.recordNumber }}</span>
    </div>
  </div>
  <div class="data-table">
    <table class="table table-bordered table-striped">
      <thead>
      <tr>
        <th>חברה מנהלת</th>
        <th>קופה בשכר</th>
        <th>סוג מוצר</th>
        <th>סוג תשלום</th>
        <th>מס' חשבון/צ'ק</th>
        <th>תאריך תשלום</th>
        <th>סכום</th>
        <th>הערות</th>
        <th>סטטוס</th>
      </tr>
      </thead>
      <tbody>
      <tr *ngFor="let payment of items">
        <td>{{payment.manufacturer.name }}</td>
        <td class="text-center grey-color">
          <i class="fa fa-info-circle" (click)="openProductDetailsDialog(payment.id)"></i>
        </td>
        <td>{{ getproductTypeLabel(payment.productType) }}</td>
        <td>{{ getPayTypeLabel(payment.kindPay) }}</td>
        <td>
          <i class="fa fa-pencil mx-2 grey-color" (click)="openBankDetailsDialog(payment)" [title]="Gettitle(payment)" ></i>
          <span class="account-number">{{payment.accountNumber}}</span>
        </td>
        <td>
          <i class="fa fa-pencil mx-2 grey-color" (click)="openDateDialog(payment)"></i>
          <span>{{payment.paymentDate | date: 'dd/MM/yyyy' }}</span>
        </td>
        <td>
          <span class="bold">{{ payment.sumFile | commatize }}</span>
          <span>&#8362;</span>
        </td>
        <td class="text-center grey-color">
          <i class="fa fa-comment" (click)="openCommentDialog(payment)" [title]="payment.remark" [ngStyle]="{'color':payment.remark!='' ? 'green' : '' }"
         ></i>
        </td>
        <td>{{ getSentToSafeBoxesLabel(payment.statusSafeBoxes) }}</td>
      </tr>
      </tbody>
    </table>
  </div>

</div>
